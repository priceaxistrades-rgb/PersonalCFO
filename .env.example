# PostgreSQL connection string from Neon, Railway, Supabase, or local Postgres
DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:5432/personal_cfo"

# Required in production. Generate with: openssl rand -base64 32
AUTH_SECRET="replace-with-a-long-random-secret"

# Optional
NODE_ENV="development"
